<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background -->
    <ImageView
        android:id="@+id/gifBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/game" />

    <!-- Foreground content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="top|center_horizontal"
        android:paddingTop="30dp"
        android:background="@android:color/transparent">

        <!-- Word Display -->
        <TextView
            android:id="@+id/currentWordDisplay"
            android:layout_width="180dp"
            android:layout_height="40dp"
            android:background="@drawable/rounded_textview"
            android:text="                      "
            android:textSize="30sp"
            android:gravity="center"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp" />

        <!-- FrameLayout to overlay bingo on prompt -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:layout_gravity="center"
            android:layout_marginTop="10dp">

            <!-- Word Grid -->
            <GridLayout
                android:id="@+id/wordGrid"
                android:layout_width="match_parent"
                android:layout_height="400dp"
                android:columnCount="4"
                android:rowCount="4"
                android:padding="10dp"
                android:background="@android:color/transparent"
                android:gravity="center" />

            <!-- Bingo animation overlay -->
            <com.airbnb.lottie.LottieAnimationView
                android:id="@+id/bingoAnimation"
                android:layout_width="300dp"
                android:layout_height="300dp"
                android:layout_gravity="center"
                app:lottie_rawRes="@raw/bingo"
                app:lottie_loop="false"
                app:lottie_autoPlay="false"
                android:visibility="gone" />
        </FrameLayout>


        <!-- Prompt Button -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/promptButton"
            android:layout_width="120dp"
            android:layout_height="120dp"
            app:lottie_rawRes="@raw/prompt"
            app:lottie_autoPlay="true"
            app:lottie_loop="true" />

        <LinearLayout
            android:id="@+id/navigationLayout"
            android:layout_width="260dp"
            android:layout_height="55dp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="40dp"
            android:orientation="horizontal"
            android:paddingHorizontal="50dp"
            android:gravity="center_vertical">

            <Button
                android:id="@+id/backButton"
                android:layout_width="0dp"
                android:layout_height="55dp"
                android:layout_weight="1"
                android:text="Back"
                android:textSize="18sp"
                android:background="@drawable/button_3d_round"/>
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
